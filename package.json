{
  "name": "ticketpilot-claudecode-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for ticketpilot-claudecode — Jira ticket-driven development automation for Claude Code",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
